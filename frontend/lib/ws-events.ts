export interface ActivityChangedData { id: string; isOpen: boolean }
export interface ScoresUpdateData    { userId: string; totalPoints: number }
export interface JobsDoneData        { jobId: string; type: string; userId: string }
